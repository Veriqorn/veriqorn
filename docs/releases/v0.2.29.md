# Veriqorn Community v0.2.29

- Publish the current Community backend and frontend as a new immutable image
  pair after consolidating the release and deployment paths in `Veriqorn/veriqorn`.
- Publish the Community API contracts and extension SDK as version `0.2.29`.
