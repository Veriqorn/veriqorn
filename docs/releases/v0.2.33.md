# Veriqorn Community v0.2.33

- Fix startup of the packaged Community backend when `extensions/manifest.json`
  has a UTF-8 byte-order mark.
- Continue publishing the canonical public images as `veriqorn-backend` and
  `veriqorn-frontend`.
