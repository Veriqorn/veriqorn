# Veriqorn Community v0.2.30

- Recreate the Community container packages from `Veriqorn/veriqorn` and
  publish a fresh immutable backend/frontend image pair.
- Publish the Community API contracts and extension SDK as version `0.2.30`.
