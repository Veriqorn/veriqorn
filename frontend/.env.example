# Backend API URL — point to your backend-v2 instance
VITE_API_URL=http://localhost:3001

# Frontend port — set PORT to customise (default: 3000)
# PORT=3000

# Application name shown in the UI
VITE_APP_NAME=Veriqorn
